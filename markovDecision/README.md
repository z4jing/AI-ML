The program markovDecision.py a4.py prints out the all the states with true utilities for each iteration and a optimal policy at the end.

Note: the program should at least run in Python 3 enviroment !
