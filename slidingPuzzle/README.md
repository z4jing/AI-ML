Instructions for running the program

python file and one script:   astar.py   runall

The program takes sample inputs puzzle1.txt and puzzle2.txt then generate solutions for the Hua Rong Dao puzzle. The required outputs would be generated in two corresponding output files.

Note: the program should at least run in Python 2.7 enviroment!

Before testing: put two initial configurations as input in puzzle1.txt puzzle2.txt and place these two input files in the same directory with the other files


To test A* search then type command:      python astar.py      in the shell.
